version: "2"
linters:
  default: standard
  settings:
    errcheck:
      exclude-functions:
        - (*github.com/jackchuka/ccli/internal/output.Printer).PrintText

formatters:
  enable:
    - goimports
    - gofmt
